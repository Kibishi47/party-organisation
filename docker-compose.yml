version: '3.8'

services:
  app:
    build: .
    container_name: party-orga-app
    ports:
      - "5173:5173"
    volumes:
      - orga-data:/app/data
    restart: always

volumes:
  orga-data:
    name: orga-data-volume
